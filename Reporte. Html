<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registro de Asignación de Equipos y Bloques</title>
  <style>
    :root {
      --navy-header: #002060;
      --blue-section: #5b9bd5;
      --border-color: #000000;
      --bg-light: #f4f7f9;
    }

    body {
      font-family: Arial, sans-serif;
      margin: 20px;
      background-color: var(--bg-light);
    }

    h1, h2 {
      text-align: center;
      color: #333;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      display: flex;
      flex-wrap: wrap;
      gap: 30px;
    }

    /* Formulario */
    .form-container {
      flex: 1;
      min-width: 320px;
      background: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .form-group {
      margin-bottom: 15px;
    }

    label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
      font-size: 14px;
    }

    input, select, button {
      width: 100%;
      padding: 9px;
      border: 1px solid #ccc;
      border-radius: 4px;
      box-sizing: border-box;
      font-size: 14px;
    }

    button.btn-add {
      background-color: #28a745;
      color: white;
      font-weight: bold;
      border: none;
      cursor: pointer;
      margin-top: 10px;
    }

    button.btn-add:hover {
      background-color: #218838;
    }

    button.btn-clear {
      background-color: #dc3545;
      color: white;
      font-weight: bold;
      border: none;
      cursor: pointer;
      margin-top: 10px;
    }

    button.btn-clear:hover {
      background-color: #c82333;
    }

    /* Tablas de la UI */
    .preview-container {
      flex: 1;
      min-width: 350px;
    }

    .custom-table {
      width: 100%;
      border-collapse: collapse;
      border: 2px solid var(--border-color);
      background-color: white;
      font-size: 13px;
    }

    .custom-table th {
      background-color: var(--navy-header);
      color: white;
      font-weight: bold;
      text-transform: uppercase;
      padding: 8px;
      border: 1px solid var(--border-color);
      text-align: center;
    }

    .custom-table td {
      border: 1px solid var(--border-color);
      text-align: center;
      padding: 6px;
      font-weight: bold;
    }

    .section-header {
      background-color: var(--blue-section);
      color: black;
      font-weight: bold;
      text-align: center;
    }

    .tech-name {
      text-transform: uppercase;
      text-align: center;
    }

    .highlight-pink {
      background-color: #fce4d6;
    }

    .actions-cell {
      width: 30px;
    }

    .btn-delete {
      background: none;
      border: none;
      color: red;
      font-weight: bold;
      cursor: pointer;
    }
  </style>
</head>
<body>

  <h1>Registro de Técnicos, Radio y Tablet por Bloque</h1>

  <div class="container">
    <!-- FORMULARIO DE INGRESO -->
    <div class="form-container">
      <h2>Ingresar Registro</h2>
      <form id="recordForm">
        <div class="form-group">
          <label for="bloque">Bloque / Zona:</label>
          <input type="text" id="bloque" placeholder="Ej: 1D, 1E, AFOROS, BUQ" required>
        </div>

        <div class="form-group">
          <label for="tecnicos">Técnico(s) (Separados por coma):</label>
          <input type="text" id="tecnicos" placeholder="Ej: TOMALA, CHONILLO" required>
        </div>

        <div class="form-group">
          <label for="radio">Radio asignada:</label>
          <input type="text" id="radio" placeholder="Ej: 4012, LP, o dejar vacío si no usa">
        </div>

        <div class="form-group">
          <label for="tablets">Tablet(s) por Técnico (Separadas por coma):</label>
          <input type="text" id="tablets" placeholder="Ej: 5, 6 (coincidente con cada técnico)">
        </div>

        <button type="submit" class="btn-add">Añadir Registro</button>
        <button type="button" class="btn-clear" onclick="limpiarTodo()">Borrar Todos los Datos</button>
      </form>
    </div>

    <!-- VISTA PREVIA FORMATO TABLA -->
    <div class="preview-container">
      <h2>Vista del Reporte</h2>
      <table class="custom-table">
        <thead>
          <tr>
            <th style="width: 45%;">BLOQUES</th>
            <th style="width: 25%;">RADIO</th>
            <th style="width: 20%;">TABBLE</th>
            <th class="actions-cell"></th>
          </tr>
        </thead>
        <tbody id="tableBody">
          <!-- Datos insertados dinámicamente -->
        </tbody>
      </table>
    </div>
  </div>

  <script>
    // Cargar datos al iniciar
    document.addEventListener("DOMContentLoaded", renderTable);

    const form = document.getElementById("recordForm");

    form.addEventListener("submit", function(e) {
      e.preventDefault();

      const bloque = document.getElementById("bloque").value.trim();
      const tecnicosStr = document.getElementById("tecnicos").value.trim();
      const radio = document.getElementById("radio").value.trim();
      const tabletsStr = document.getElementById("tablets").value.trim();

      // Convertir strings a arrays
      const tecnicos = tecnicosStr.split(',').map(t => t.trim()).filter(t => t !== "");
      const tablets = tabletsStr ? tabletsStr.split(',').map(t => t.trim()) : [];

      if (tecnicos.length === 0) return;

      const newEntry = {
        id: Date.now(),
        bloque: bloque.toUpperCase(),
        tecnicos: tecnicos,
        radio: radio ? radio.toUpperCase() : "",
        tablets: tablets
      };

      saveEntry(newEntry);
      form.reset();
      renderTable();
    });

    function getStoredData() {
      const data = localStorage.getItem("registros_bloques");
      return data ? JSON.parse(data) : getInitialData();
    }

    function saveEntry(entry) {
      const currentData = getStoredData();
      currentData.push(entry);
      localStorage.setItem("registros_bloques", JSON.stringify(currentData));
    }

    function deleteEntry(id) {
      let currentData = getStoredData();
      currentData = currentData.filter(item => item.id !== id);
      localStorage.setItem("registros_bloques", JSON.stringify(currentData));
      renderTable();
    }

    function limpiarTodo() {
      if (confirm("¿Está seguro de borrar todos los registros almacenados?")) {
        localStorage.removeItem("registros_bloques");
        renderTable();
      }
    }

    // Datos iniciales exactamente como la imagen de muestra
    function getInitialData() {
      return [
        { id: 1, bloque: "1D", tecnicos: ["TOMALA", "CHONILLO"], radio: "4012", tablets: ["5", "6"] },
        { id: 2, bloque: "1E", tecnicos: ["SOLANO", "MESA"], radio: "4029", tablets: ["9", "7"] },
        { id: 3, bloque: "1F", tecnicos: ["TOCTO", "TORRES"], radio: "4028", tablets: ["1", "11"] }, // Torres usa radio 1018 según la foto
        { id: 4, bloque: "1G", tecnicos: ["TARIFEÑO", "PAREDES"], radio: "LP", tablets: ["13", "8"] },
        { id: 5, bloque: "1H", tecnicos: ["PACHAY", "TENORIO"], radio: "4025", tablets: ["10", "4"] },
        { id: 6, bloque: "1I", tecnicos: ["MINA", "SANCHEZ"], radio: "4078", tablets: ["12", "3"] },
        { id: 7, bloque: "AFOROS", tecnicos: ["FAJARDO"], radio: "4071", tablets: [""] },
        { id: 8, bloque: "BUQ", tecnicos: ["CEDEÑO", "GERRERO", "PIBAQUE"], radio: "4015", tablets: ["", "", ""] }
      ];
    }

    function renderTable() {
      const data = getStoredData();
      const tbody = document.getElementById("tableBody");
      tbody.innerHTML = "";

      data.forEach(group => {
        const rowCount = group.tecnicos.length;

        // 1. Fila del encabezado de Bloque
        const headerRow = document.createElement("tr");
        headerRow.innerHTML = `<td colspan="4" class="section-header">${group.bloque}</td>`;
        tbody.appendChild(headerRow);

        // 2. Filas de cada técnico
        group.tecnicos.forEach((tecnico, index) => {
          const row = document.createElement("tr");

          // Estilo rosado si es el bloque BUQ (CEDEÑO/GERRERO)
          const isPink = group.bloque === "BUQ" && index < 2;
          const bgClass = isPink ? "highlight-pink" : "";

          // Nombre del técnico
          let html = `<td class="tech-name ${bgClass}">${tecnico}</td>`;

          // Agrupar celda de RADIO (rowspan) para el primer elemento
          if (index === 0) {
            html += `<td rowspan="${rowCount}" style="vertical-align: middle;">${group.radio}</td>`;
          }

          // Celda de TABLET individual
          const tabVal = group.tablets[index] !== undefined ? group.tablets[index] : "";
          html += `<td>${tabVal}</td>`;

          // Botón para eliminar el grupo completo
          if (index === 0) {
            html += `<td rowspan="${rowCount}" class="actions-cell" style="vertical-align: middle;">
                      <button class="btn-delete" onclick="deleteEntry(${group.id})" title="Eliminar este grupo">✕</button>
                    </td>`;
          }

          row.innerHTML = html;
          tbody.appendChild(row);
        });
      });
    }
  </script>
</body>
</html>
